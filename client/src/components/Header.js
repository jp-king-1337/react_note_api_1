function Header(propsObj) {
  return (
    <header className="row justify-between align-center">
      <h3>Logo</h3>
      <nav>
        <a href="#">Home</a>
        <a href="#">About</a>
        <a href="#">Contact</a>
      </nav>
    </header>
  )
}

export default Header;